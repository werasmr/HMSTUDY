# Netlify — только фронтенд
# Backend и база данных деплоятся отдельно (см. DEPLOY-RU.md)

[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# React Router — все пути ведут на index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
